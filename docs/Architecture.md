# Architecture

This project uses a small Express.js backend to provide resume analysis endpoints. The backend is structured with controllers, services, models, and middleware to allow easy extension.

Future extensions:
- Add a persistence layer (MongoDB/Postgres)
- Add a ML-based resume parser service
- Add authentication and rate limiting
